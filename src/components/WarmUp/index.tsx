import * as React from 'react';
import classNames from 'classnames';

import styles from './WarmUp.module.css';

export default function WarmUp() {
  return (
    <div className={classNames(styles.WarmUp)} />
  );
}
