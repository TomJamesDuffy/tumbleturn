import * as React from 'react';
import classNames from 'classnames';

import styles from './Set.module.css';

export default function Set() {
  return (
    <div className={classNames(styles.Set)} />
  );
}
