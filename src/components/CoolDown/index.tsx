import * as React from 'react';
import classNames from 'classnames';

import styles from './CoolDown.module.css';

export default function Cooldown() {
  return (
    <div className={classNames(styles.Cooldown)} />
  );
}
